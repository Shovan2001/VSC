//Check if a linked list is a circular linked list.
/*
if(head==NULL)
   return 1;
   while(temp!=NULL)
   {
       if(temp->next==head)
       {
           return 1;
       }
       temp=temp->next;
   }
   return 0;
*/ 